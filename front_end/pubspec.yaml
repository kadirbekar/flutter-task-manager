name: getx_nodejs_task_manager
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  get: ^4.3.0
  json_annotation: ^4.0.0
  get_it: ^7.2.0
  google_fonts: ^2.1.0
  jiffy: ^4.1.0
  flutter_svg: ^0.22.0
  vexana: ^2.2.2

dev_dependencies:
  json_serializable: ^4.1.4
  build_runner: ^2.0.6
  
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/svg/
